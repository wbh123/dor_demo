# 第一阶段最终收尾记录

> 日期：2026-08-01  
> 阶段：第一阶段——核心选寝闭环

## 1. 用户要求

确认第一阶段状态，自动完成遗留修复并提交主分支，随后无人值守开启第二阶段开发。补充要求为先恢复Three.js斜视相机和纵向床位，并确认管理端是否支持逐房间床位布局和上下铺位置配置。

## 2. 状态核对

第一阶段核心业务、数据库、OpenAPI、前端和真实运行门禁已经完成，但文档存在以下漂移：

- `AGENTS.md`和阶段索引仍写第一阶段开发中；
- 部分文档只列Flyway V1至V3，遗漏V4；
- 部分文档仍描述主动创建命名队伍；
- 部分文档仍建议让后端Flyway加载开发重复迁移；
- Three.js场景说明仍保留旧的门口正视2×2布局描述；
- 第二阶段没有被标记为当前开发阶段。

## 3. 收尾修改

- 将第一阶段统一标记为“已完成并最终冻结”；
- 将第二阶段统一标记为“开发中”；
- 将V4纳入第一阶段数据库冻结基线；
- 将组队规则统一为邀请式组队；
- 将Three.js斜视相机、纵向床位和移动端能力纳入冻结说明；
- 将本地测试数据加载方式统一为`reset-local-environment.sh`；
- 明确Flyway只执行正式迁移，开发数据通过MySQL客户端导入；
- 增加阶段状态自动化回归测试。

## 4. 布局配置能力核对

第一阶段数据库仅保存：

```text
bed_type
position_index
bed_frame_id
```

没有保存三维坐标、旋转角或布局模板。管理端OpenAPI和页面也只允许编辑房型、容量、性别和运行状态。

因此第一阶段不能：

- 为每个房间独立设置床位三维布局；
- 单独指定上下铺位于房间中的位置；
- 保存床架朝向。

第二阶段将首先实现逐房间布局配置，并要求上下铺上下层共享平面坐标和朝向。

## 5. 验收要求

收尾分支必须重新通过：

- 基础设施和阶段状态测试；
- 数据库、OpenAPI、后端、前端和体验回归测试；
- Java全模块构建；
- Vue、TypeScript、Three.js和Vite生产构建；
- MySQL、Redis、Flyway、Spring Boot和完整HTTP主流程。

不得使用第一阶段历史运行结果代替本次最新提交验证。
