# Wust Dormitory Select 项目智能体规则

> 创建日期：2026-08-01  
> 项目：武汉科技大学学生宿舍智能选择系统  
> 当前阶段：第一阶段——核心选寝闭环开发

## 1. 项目目标与边界

系统用于高校新生宿舍资源维护、选寝批次管理、学生自主选寝、组队选寝、随机选择、系统分配、生活习惯匹配、实时床位状态、结果导出和操作审计。

系统不得：

- 根据生活习惯分数评价学生人格、品德或心理状态；
- 向其他学生公开联系方式、证件号码、家庭信息或完整档案；
- 使用大语言模型替代确定性匹配和分配规则；
- 仅依赖Redis认定最终床位归属；
- 绕过学校规则自动改变学生住宿资格；
- 在没有审计记录的情况下修改最终分配结果；
- 允许男女学生分配到同一房间。

推荐结果只能表示“按当前问卷和权重计算出的相似程度”，不得表述为一定相处融洽。

## 2. 当前阶段状态

```text
准备阶段：已完成
第一阶段：开发中，当前已建立数据库与测试数据基线
第二阶段：未开始
第三阶段：未开始
```

第一阶段继续按以下顺序推进：

1. 数据库和测试数据基线；
2. 统一响应、错误码、请求编号和认证；
3. 学生、组织和宿舍资源；
4. 批次、资格、问卷和匹配；
5. 个人临时占用和最终提交；
6. 房间级服务器发送事件；
7. 组队选寝；
8. 随机选择、统一分配、调整和导出；
9. 全链路验收和阶段冻结。

## 3. 固定业务规则

- 学院管理员与宿舍管理员现阶段合并为业务管理员；
- 系统按单实例部署设计；
- 学生选寝包含自主选择、随机选择和系统统一分配；
- 选寝页面支持个人选择和组队选择；
- Redis只保存短期床位占用、热点状态和可重建缓存；
- MySQL保存最终分配、批次规则、历史和审计；
- 生活习惯匹配使用可解释、可复现、可测试的确定性算法；
- 学生打开房间页面后建立房间级服务器发送事件连接，离开页面时关闭；
- 服务端发送心跳并清理失效连接。

## 4. 房型与男女宿舍规则

### 4.1 当前需求

- 男生宿舍当前为五人间；
- 男生五人间包含3个上床下桌和1组上下铺；
- 女生宿舍当前为四人间；
- 女生四人间包含4个上床下桌。

### 4.2 可扩展规则

当前房型组合不是永久限制。后续学校调整时，可以出现：

- 男生四人间和男生五人间混合；
- 女生四人间和女生五人间混合；
- 同一楼栋内不同房间采用不同房型。

必须始终满足：

- 每个房间具有固定男寝或女寝属性；
- `room.gender_restriction` 只能为 `M` 或 `F`；
- 男女学生不得混住同一房间；
- 房型、容量和床位布局必须一致；
- 每个床位都是独立资源；
- 上下铺通过同一床架关联；
- 管理员可以在选寝批次中按楼栋、房间和床位精确设置可选范围；
- 学生性别必须与候选房间性别一致。

宿舍楼层面可以保留中性或混合用途标记，但房间本身不得使用 `ANY` 表示可混住。

## 5. 总体架构边界

```text
Vue 3
  ↓ REST + 服务器发送事件
Spring Boot单体后端
  ├── MySQL
  └── Redis
```

现阶段禁止引入：

- 服务注册中心；
- 消息队列；
- Kubernetes；
- 为单实例系统设计复杂分布式事务；
- 前端直连数据库或Redis；
- 将选寝核心一致性放在浏览器端；
- 为了架构形式拆分微服务。

## 6. Java后端规则

保持多模块结构：

```text
backend-java/
├── model/
├── client/
├── server/
└── starter/
```

- 对外接口先修改OpenAPI，再生成接口和数据传输对象；
- 控制器只负责协议转换、权限入口和调用业务服务；
- 复杂资格、分配和一致性逻辑必须进入业务服务；
- 业务服务不得长期传递OpenAPI数据传输对象作为领域事实；
- 数据库约束是最终一致性的最后防线；
- 同一学生在同一批次只能有一个最终床位；
- 同一床位在同一批次只能分配给一个学生；
- 最终提交必须校验批次、资格、房间性别、床位状态、临时占用令牌和队伍状态；
- 任何分配调整必须记录操作者、原因、调整前后结果和时间；
- Java根包统一为 `com.wust.dormitory`；
- Java包声明必须与目录一致；
- MyBatis映射目录必须与运行配置一致。

## 7. Flyway与数据库规则

开发阶段数据库结构只通过Flyway演进：

```text
backend-java/server/src/main/resources/db/migration/
```

规则：

- 已执行迁移不得修改；
- 结构变化新增版本迁移；
- 正式应用默认只执行正式迁移；
- 开发测试数据放在 `src/test/resources/db/dev-migration`；
- 生产环境不得加载开发测试数据；
- 空库迁移和已有数据库升级都必须测试；
- 每次数据库变化同步更新数据字典和阶段文档；
- 第一阶段全部开发和验收完成后执行固化工具；
- 固化SQL只合并正式版本迁移，不包含测试数据；
- 原Flyway迁移必须继续保留。

固化命令：

```bash
python scripts/db/build_frozen_baseline.py
```

## 8. Redis与并发规则

- 临时占用键包含批次和床位标识；
- 临时占用必须有过期时间；
- 占用使用原子命令或Lua脚本；
- 禁止先查询再写入形成竞态；
- 续期只允许原令牌持有者；
- 释放必须比较令牌；
- 组队多床位占用必须整体成功或整体失败；
- Redis不可用时不得绕过并发校验；
- 最终提交必须由数据库事务和唯一约束再次确认；
- 推送消息只表示状态变化，不是最终事实。

## 9. 服务器发送事件规则

- 连接以批次和房间为订阅范围；
- 学生只能订阅有资格访问的房间；
- 事件至少包含事件编号、房间编号、版本号、类型和发生时间；
- 必须支持心跳、连接关闭、超时清理和浏览器重连；
- 推送中不得发送其他学生敏感信息；
- 推送失败不得影响数据库事务结果；
- 重连后前端必须重新获取权威快照。

## 10. 匹配算法规则

匹配算法至少包含：

1. 硬约束过滤：批次、性别、校区、资格、房间范围和容量；
2. 问卷特征标准化；
3. 可配置权重；
4. 学生相似度；
5. 房间平均匹配、最低成员匹配和冲突惩罚；
6. 候选缩小和精确重排；
7. 可解释结果。

算法修改必须提供固定输入输出测试，不能只依靠人工观察。

## 11. 前端规则

- 前端只调用Spring Boot；
- 关键按钮处理重复点击、请求中状态和失败反馈；
- 床位状态明确区分可选、临时占用、已分配、禁用和不可访问；
- 四人间和五人间都必须按真实床位布局展示；
- 页面离开时主动关闭服务器发送事件连接；
- 断线重连后重新获取房间快照；
- 不在浏览器持久化其他学生敏感资料；
- 正式分配结果以服务端和数据库返回为准。

## 12. 测试底线

每个核心功能至少覆盖：

- 正常主流程；
- 参数错误；
- 未登录和无权限；
- 批次未开始、暂停、结束或关闭；
- 学生无资格或已完成选寝；
- 学生与房间性别不一致；
- 房型、容量和床位布局不一致；
- 床位不存在、禁用、已占用或临时占用冲突；
- Redis超时、令牌不匹配和占用过期；
- 数据库唯一约束冲突；
- 组队成员变化、性别一致性、队伍锁定和容量不足；
- 服务器发送事件连接、心跳、重连和资源清理；
- 匹配算法固定样例、边界和性能；
- OpenAPI与实现一致；
- 前端类型检查、测试和生产构建；
- Flyway空库迁移、升级迁移和文档同步。

不得根据旧测试结果声称新提交仍然通过。

## 13. 文档与记录

每次开发记录：

- 用户原始要求；
- 日期时间；
- 需求解释和自主决策；
- 修改文件；
- 数据库和接口变化；
- 测试命令与真实结果；
- 已知限制；
- 后续事项。

项目文档放在 `docs/`，过程记录放在 `records/`。

发生冲突时优先级为：

1. 用户最近一次明确指令；
2. 根目录和子目录 `AGENTS.md`；
3. 当前阶段文档；
4. OpenAPI、Flyway迁移和当前代码事实；
5. 总体需求和系统设计；
6. README；
7. 历史记录。

## 14. Git提交

- 每个提交只处理一个清晰主题；
- 不混入无关重构；
- 提交信息推荐 `<type>(<scope>): <中文摘要>`；
- 修复缺陷必须记录根因；
- 不通过降低测试标准绕过问题；
- 阶段完成后形成验收记录和冻结说明；
- 用户要求限制提交数时，使用主题分支和Squash合并控制主分支提交数量。
