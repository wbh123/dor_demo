# Wust Dormitory Select 项目智能体规则

> 项目：武汉科技大学学生宿舍智能选择系统  
> 最后更新：2026-08-01  
> 当前状态：第一阶段：已完成并最终冻结；第二阶段：开发中

## 1. 项目目标与安全边界

系统面向高校新生宿舍资源维护、生活习惯问卷、个人或邀请式组队选寝、随机推荐、统一分配、实时床位状态、结果导出和操作审计。

系统不得：

- 根据问卷分数评价学生人格、品德或心理状态；
- 向其他学生公开联系方式、证件信息或完整问卷；
- 使用大语言模型替代确定性匹配和分配规则；
- 仅依赖 Redis 判断最终床位归属；
- 绕过住宿资格、性别和批次范围规则；
- 在没有原因、历史和审计记录时修改最终分配；
- 允许男女学生分配到同一房间。

推荐结果只能表示“按当前问卷与权重计算出的相似程度”。

## 2. 阶段状态

```text
准备阶段：已完成
第一阶段：已完成并最终冻结
第二阶段：开发中
第三阶段：待评估
```

第一阶段冻结能力包括：

- OpenAPI 驱动的认证、管理端、学生端与实时接口；
- 专业、学生、宿舍、批次、问卷和匹配；
- 个人选寝与邀请式组队选寝；
- Redis 临时占用、MySQL 最终确认和房间级服务器发送事件；
- 统一分配、导出、人工调整与审计；
- Three.js 三维床位选择、下拉框联动和移动端适配；
- Flyway V1 至 V4、开发测试数据与固化结构快照。

第二阶段按以下优先级推进：

1. 每个房间独立床位布局与上下铺位置配置；
2. 匹配权重、冲突解释和管理界面；
3. 批次复制、规则模板与复杂组队异常处理；
4. 导入质量、安全、统计、性能与恢复能力；
5. 分配优化和公平性评估。

## 3. 固定业务规则

- 学院管理员与宿舍管理员现阶段合并为业务管理员；
- 单实例部署，不引入不必要的微服务、注册中心和消息队列；
- 学生可自主选择、随机推荐或由管理员统一分配；
- 组队采用“输入学号直接邀请”，系统内部自动建立小组；
- 队伍名称和内部编号不在学生前端展示；
- 同一学生同一时刻只能参加一个活动批次；
- 房间必须固定为男寝或女寝，男女不得混住；
- 男生当前测试配置为五人间，女生当前测试配置为四人间，但模型允许未来混合房型；
- 上下铺通过同一床架关联，每个床位仍是独立资源。

## 4. 架构边界

```text
Vue 3 + TypeScript + Three.js
  ↓ REST + 服务器发送事件
OpenAPI 3 契约
  ↓ 自动生成 Java API、数据传输对象与 TypeScript 类型
Spring Boot 4 单体后端
  ├── MySQL 8.4：最终业务事实
  └── Redis 7.4：令牌、临时占用和可重建状态
```

禁止：

- 前端直连 MySQL 或 Redis；
- 前端自行计算正式分配结果；
- 为单实例系统设计复杂分布式事务；
- 将临时缓存作为最终事实；
- 绕过 OpenAPI 手写对外 Controller 路由。

## 5. OpenAPI 与 Java规则

后端保持多模块：

```text
backend-java/
├── model/
├── client/
├── server/
└── starter/
```

- 所有对外接口先修改 OpenAPI，再生成 `*Api` 和数据传输对象；
- Controller 只负责协议转换、权限入口和调用业务服务；
- 复杂资格、布局、匹配、分配和一致性逻辑进入业务服务；
- Java 根包统一为 `com.wust.dormitory`；
- 包声明、目录和 MyBatis 映射位置必须一致；
- 新接口必须通过契约引用、操作编号唯一性和生成编译测试。

## 6. 数据库与 Flyway规则

正式迁移目录：

```text
backend-java/server/src/main/resources/db/migration/
```

当前正式迁移：

```text
V1__create_phase1_schema.sql
V2__enforce_fixed_room_gender.sql
V3__normalize_major_and_minimize_student.sql
V4__refine_questionnaire_and_active_batch_rules.sql
```

规则：

- 已执行迁移不得修改，只能新增更高版本；
- 生产应用只加载正式迁移；
- 开发测试数据位于 `src/test/resources/db/dev-migration`，由本地重建脚本通过 MySQL 客户端导入，不写入 Flyway 历史；
- 数据库变化同步更新数据字典、固化 `schema.sql` 和漂移测试；
- 最终分配、床位和学生唯一约束不得弱化；
- 第二阶段独立床位布局必须使用 V5 或更高版本迁移。

固化命令：

```bash
python scripts/db/build_frozen_baseline.py
```

本地从零重建：

```bash
bash scripts/dev/reset-local-environment.sh
```

## 7. Redis、并发与实时推送

- 临时占用键必须包含批次和床位标识，并设置过期时间；
- 占用、释放和续期必须使用原子命令或 Lua，并校验令牌；
- 组队多床位占用必须整体成功或整体失败；
- Redis 不可用时不得绕过并发校验；
- 最终提交必须由数据库事务和唯一约束再次确认；
- 服务器发送事件按批次和房间订阅，必须支持心跳、关闭、重连和资源清理；
- 推送只表示状态变化，不是最终事实。

## 8. 匹配与分配规则

匹配算法必须包含硬约束过滤、标准化特征、可配置权重、冲突惩罚、候选排序和可解释结果。算法变更必须有固定输入输出测试并记录版本。

统一分配必须：

- 优先保持已确认小组完整；
- 不拆散锁定小组；
- 对无法分配的学生给出稳定原因；
- 保存预演参数、随机种子、正式结果和审计；
- 第二阶段优化不得破坏第一阶段确定性与可追溯性。

## 9. 前端与三维布局规则

- 页面只调用 Spring Boot 接口；
- 关键按钮处理重复点击、加载和失败反馈；
- 床位状态明确区分可选、本人临时占用、他人占用、已分配和禁用；
- Three.js 场景与下拉框共享同一选择状态；
- 页面离开时关闭服务器发送事件连接；
- WebGL 不可用时仍可通过下拉框完成选择；
- 当前第一阶段三维布局由 `position_index` 固定映射；
- 第二阶段新增逐房间布局配置后，必须优先读取数据库布局并保留旧数据回退规则；
- 管理端不得让上下铺上下层保存为不同平面位置。

## 10. 测试底线

每项核心功能至少覆盖：

- 正常主流程、参数错误、未登录和无权限；
- 批次状态、资格、性别、房间范围和床位状态；
- Redis 超时、令牌不匹配、占用冲突和数据库唯一冲突；
- 组队邀请、接受、锁定、成员变化和整体占用；
- 服务器发送事件连接、心跳、重连与清理；
- 匹配算法固定样例、边界与性能；
- OpenAPI 生成、Java 构建、TypeScript 检查和 Vue 生产构建；
- Flyway 空库迁移、升级迁移、固化结构漂移；
- MySQL、Redis、Spring Boot 和完整 HTTP 主流程。

不得根据旧测试结果声称新提交仍然通过。

## 11. 文档、记录与 Git

每次开发记录用户要求、日期时间、自主决策、修改文件、数据库和接口变化、测试结果、已知限制与后续事项。

发生冲突时优先级：

1. 用户最近一次明确指令；
2. 根目录和子目录 `AGENTS.md`；
3. 当前阶段设计、验收和冻结文档；
4. OpenAPI、Flyway、自动化测试和当前代码事实；
5. 总体需求与系统设计；
6. README；
7. 历史记录。

每个提交只处理一个主题；缺陷修复必须记录根因；阶段功能通过主题分支开发并使用 Squash 控制 `main` 提交数量。
