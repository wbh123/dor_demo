# 公开脱敏开发仓库智能体规则

> 仓库用途：脱敏功能开发、自动化测试和 GitHub Actions 持续集成验证  
> 最后更新：2026-08-03  
> 状态：强制执行

## 1. 仓库定位

本仓库不是完整项目事实源，也不是最终交付仓库。

本仓库仅允许：

- 开发已经完成脱敏的通用功能代码；
- 编写单元测试、接口测试、前端测试和构建测试；
- 维护 GitHub Actions 工作流；
- 保存只包含占位值的示例配置；
- 保存最小化公开说明、许可证和安全规则。

任何智能体、开发人员或自动化工具在修改本仓库前，必须先遵守本文件。

## 2. 数据库硬性禁止规则

不得在本仓库新增、复制、恢复、生成或提交：

- Flyway、Liquibase 等正式数据库迁移文件；
- 建表、改表、索引、约束、触发器和存储过程脚本；
- `schema.sql`、数据库结构快照和数据库备份；
- 数据字典、实体关系图、表关系或字段说明；
- 真实初始化数据、测试数据转储和审计数据；
- 能够反推出真实业务表结构的完整持久化实体、映射、测试夹具或模拟数据库；
- 数据库连接地址、账号、密码和生产参数。

涉及数据库的功能只能通过以下方式在本仓库验证：

- 接口或仓储抽象；
- 模拟仓储；
- 内存对象；
- 不反映真实数据库结构的最小匿名测试模型；
- 对外部依赖进行测试替身或桩实现。

正式数据库迁移、数据字典和结构验证只能在非公开完整仓库中完成。

## 3. 文档硬性禁止规则

不得在本仓库新增或提交：

- 完整需求分析、系统设计和开发阶段文档；
- 验收材料、甲方材料、会议纪要和内部沟通记录；
- `docs/`、`records/` 或等价的内部项目资料目录；
- 部署拓扑、生产运维、安全处置和内部故障记录；
- 未公开功能路线图、内部决策和私有仓库文件清单；
- 可识别学校、学院、校区、人员和内部业务流程的信息。

允许存在的文档仅限：

- `README.md`；
- `AGENTS.md`；
- 许可证；
- 安全说明；
- GitHub 模板和持续集成使用说明。

上述公开文档只能描述本仓库自身的通用用途、安全边界和贡献要求，不得承载完整项目设计信息。

## 4. 敏感信息规则

严禁提交：

- 真实学生、教师、管理员或其他人员信息；
- 学校、学院、校区、真实系统名称和内部品牌标识；
- 密钥、令牌、密码、证书、私钥和真实环境变量；
- 生产日志、错误转储、导出文件和截图中的敏感内容；
- 内部域名、服务器地址、数据库地址和监控地址；
- 非公开仓库访问凭据或可用于绕过权限的信息。

示例数据必须完全虚构、不可识别，并且不能用于推断真实数据库结构或业务规模。

## 5. 开发流程

每项功能按以下顺序推进：

```text
确认代码已经脱敏
→ 创建主题分支
→ 实现功能和测试
→ 执行静态检查、构建和自动化测试
→ 通过 GitHub Actions
→ 记录提交 SHA 和持续集成结果
→ 由维护人员选择性迁移到非公开完整仓库
```

本仓库不得：

- 直接同步非公开仓库的完整目录；
- 复制非公开仓库 Git 历史；
- 使用整仓覆盖方式进行迁移；
- 为了测试方便恢复被删除的数据库或文档文件；
- 将本仓库持续集成通过描述为完整项目已经交付。

## 6. 接口与代码规则

- 代码应使用通用、匿名化名称；
- 配置文件只使用占位值；
- 日志不得输出凭据、连接串、个人数据或内部路径；
- 对外接口样例不得包含真实业务标识；
- 测试不得依赖非公开文件、私有网络或真实数据库；
- 新依赖必须能够在公开 GitHub Actions 环境中安全安装；
- 不得通过注释、变量名、测试名称或提交信息泄露已脱敏内容。

## 7. GitHub Actions 规则

- 工作流只能使用公开安全的测试依赖和占位配置；
- 密钥必须通过 GitHub Secrets 管理，不得写入仓库；
- 工作流日志不得打印 Secrets、连接串、真实数据或内部地址；
- 测试失败不得通过删除测试、放宽断言或捕获全部异常规避；
- 只能根据当前提交实际运行的结果声明通过；
- 没有日志或工作流未执行时，不得声称已经验证。

## 8. 提交前强制检查

```text
[ ] 不含数据库迁移、建表脚本、结构快照、数据字典或实体关系图
[ ] 不含 docs、records、需求、设计、验收和内部记录
[ ] 不含学校、校区、人员、真实系统名称和内部品牌标识
[ ] 不含真实数据、生产配置、日志、密钥和内部地址
[ ] 测试数据不能反推真实数据库结构或业务规模
[ ] README、AGENTS 和 GitHub 模板只描述公开仓库规则
[ ] GitHub Actions 日志不会输出敏感信息
[ ] 当前提交已经执行必要测试，未复用旧结果
```

发现任何疑似敏感内容时，默认停止提交并删除该内容；是否可公开存在疑问时，按禁止公开处理。

## 9. 冲突优先级

发生规则冲突时，按以下顺序处理：

1. 用户最近一次明确指令；
2. 本 `AGENTS.md`；
3. `README.md`；
4. 自动化测试和当前代码事实；
5. 历史提交信息。

任何历史文件、旧代码或旧提交都不能作为恢复数据库结构或内部文档的理由。
