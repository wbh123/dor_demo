# Wust Dormitory Select 项目智能体规则

> 创建日期：2026-08-01  
> 项目：武汉科技大学学生宿舍智能选择系统  
> 当前阶段：准备阶段——工程检查与文档基线

## 1. 项目目标与边界

系统用于高校宿舍资源维护、选寝批次管理、学生自主选寝、组队选寝、随机选择、统一随机分配、生活习惯匹配、实时床位状态、结果导出和操作审计。

系统不用于：

- 根据生活习惯分数评价学生人格、品德或心理状态；
- 向其他学生公开联系方式、证件号码、家庭信息或完整个人档案；
- 使用大语言模型替代确定性分配规则；
- 仅依赖 Redis 认定最终床位归属；
- 绕过学校规则自动改变学生住宿资格；
- 在没有审计记录的情况下修改最终分配结果。

推荐结果只能表示“按当前问卷和权重计算出的相似程度”，不得表述为一定相处融洽。

## 2. 当前阶段状态

```text
准备阶段：工程修复、文档、环境和测试入口，允许开发
第一阶段：核心业务闭环，未开始
第二阶段：完善与质量提升，未开始
第三阶段：可选增强功能，未开始
```

准备阶段只建立可靠工程和设计基线，不应假装正式业务功能已经完成。

## 3. 固定业务规则

- 学院管理员与宿舍管理员现阶段合并为统一管理角色；
- 系统按单实例部署设计；
- 学生选寝分为自主选择和随机选择，未完成学生可进入统一随机分配；
- 选寝页面必须支持个人选择和组队选择；
- 五人间包含五个独立床位：三张上床下桌床位，加同一床架的一张上铺和一张下铺；
- 学生进入选寝页面后建立服务器发送事件连接，退出页面时主动关闭；
- 推送按房间维度组织，服务端定时发送心跳并清理失效连接；
- Redis 只保存短时床位占用、热点房间状态和可重建缓存；
- 数据库保存最终选寝事实、批次规则、审计和历史结果；
- 生活习惯匹配不使用大语言模型，应使用可解释、可测试的确定性算法。

## 4. 总体架构边界

```text
Vue 3
  ↓ REST + 服务器发送事件
Spring Boot 单体后端
  ├── MySQL
  └── Redis
```

禁止在现阶段引入：

- 服务注册中心；
- 消息队列；
- Kubernetes；
- 为单实例系统设计复杂分布式事务；
- 前端直连数据库或 Redis；
- 将选寝核心一致性放在浏览器端；
- 为了“架构先进”无依据拆分微服务。

## 5. Java 后端规则

保持以下多模块结构：

```text
backend-java/
├── model/
├── client/
├── server/
└── starter/
```

- 对外接口先修改 OpenAPI 契约，再生成接口和数据传输对象；
- 控制器负责协议转换、权限入口和调用业务服务，不直接编写复杂分配逻辑；
- 业务服务不得把 OpenAPI 数据传输对象作为领域事实长期传递；
- 数据库约束是最终一致性的最后防线；
- 同一学生在同一有效批次只能拥有一个最终床位；
- 同一床位在同一有效批次只能分配给一个学生；
- 提交选寝时必须校验批次状态、学生资格、临时占用令牌、床位可用性和队伍状态；
- 任何分配调整都必须记录操作者、原因、调整前后结果和时间；
- Java 包声明必须与源码目录一致，根包统一为 `com.wust.dormitory`；
- MyBatis 映射文件目录必须与 `mapper-locations` 配置一致。

## 6. Redis 与并发规则

- 临时占用键必须包含批次和床位标识；
- 临时占用必须设置明确过期时间；
- 占用操作使用原子命令或 Lua 脚本，禁止先查询再写入形成竞态；
- 续期只允许占用令牌持有者执行；
- 释放时必须校验令牌，禁止删除他人的占用；
- Redis 不可用时不得直接跳过并发校验，应返回稳定错误或进入受控降级；
- 最终提交必须由数据库事务和唯一约束再次确认；
- 推送消息只是状态提示，前端收到消息后仍应按需要重新获取权威状态。

## 7. 服务器发送事件规则

- 连接以批次和房间为订阅范围；
- 学生只能订阅当前批次允许访问的房间；
- 服务端事件至少包含事件编号、房间编号、版本号、事件类型和发生时间；
- 必须支持心跳、连接关闭、超时清理和浏览器重连；
- 不在推送中发送其他学生的敏感信息；
- 推送失败不得影响数据库事务结果；
- 前端不得把本地推送状态作为最终选寝依据。

## 8. 匹配算法规则

匹配算法至少包含：

1. 硬约束过滤：批次、性别、校区、住宿资格、房间容量和管理规则；
2. 问卷特征标准化：将作息、睡眠、卫生、空调、噪声、学习、社交等转换为数值或枚举特征；
3. 权重配置：区分关键冲突项和一般偏好项；
4. 学生相似度：使用加权距离或加权相似度；
5. 房间评分：考虑平均相似度、最差成员匹配、空床数量和床位偏好；
6. 候选缩小与精确重排：先按硬约束和特征桶筛选，再对少量候选精确计算；
7. 解释输出：展示主要匹配项与可能冲突项，不输出黑箱结论。

算法修改必须提供固定输入输出测试，不能只通过人工观察判断效果。

## 9. 前端规则

- 前端只调用 Spring Boot；
- 所有关键按钮必须处理重复点击、请求中状态和失败反馈；
- 选寝页面应明确区分可选、临时占用、已选、禁用和不可访问状态；
- 五人间床位布局必须能表达三张上床下桌以及独立上下铺；
- 页面关闭或路由离开时主动关闭服务器发送事件连接；
- 断线重连后重新获取房间权威快照；
- 不在浏览器持久化其他学生的敏感资料；
- 正式分配结果必须以服务端返回为准。

## 10. 测试底线

每个核心功能至少覆盖：

- 正常主流程；
- 参数错误；
- 未登录与无权限；
- 批次未开始、已结束或已关闭；
- 学生无资格或已完成选寝；
- 床位不存在、禁用、已占用和临时占用冲突；
- Redis 超时、令牌不匹配和占用过期；
- 数据库唯一约束冲突；
- 组队成员变化、队伍锁定和容量不足；
- 服务器发送事件连接、心跳、断线重连和资源清理；
- 匹配算法固定样例、边界值和性能；
- OpenAPI 与实现一致；
- 前端类型检查、测试和生产构建；
- 文档、迁移和记录同步。

不得根据旧测试结果声称新提交仍然通过。

## 11. 文档和记录

每次开发应记录：

- 用户原始提示词；
- 日期时间；
- 需求解释和自主决策；
- 修改文件；
- 数据库或接口变化；
- 测试命令与真实结果；
- 已知限制；
- 后续事项。

项目级文档放在 `docs/`，开发过程记录放在 `records/`。发生冲突时，优先级为：

1. 用户最近一次明确指令；
2. 根目录和子目录 `AGENTS.md`；
3. 当前阶段文档；
4. OpenAPI、数据库迁移和当前代码事实；
5. README；
6. `records/` 历史记录。

## 12. Git 提交

- 每个提交只处理一个清晰主题；
- 不混入无关重构；
- 提交信息推荐使用 `<type>(<scope>): <中文摘要>`；
- 修复缺陷时记录根因，不通过降低测试标准绕过；
- 阶段完成后形成验收记录并冻结核心边界。
