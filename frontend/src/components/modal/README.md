# 公共模态组件

业务页面中的真实模态窗口统一使用 `AppModal`，确认流程统一使用基于它实现的 `AppConfirmDialog`。

- 下拉菜单、气泡提示、三维标签和普通悬浮通知不属于模态窗口。
- 禁止业务页面复制完整的固定定位遮罩结构。
- `AppModal` 负责层级、焦点、页面滚动锁定、关闭行为和移动端安全边距。
- `AppConfirmDialog` 负责确认词、原因、异步提交和错误展示。
