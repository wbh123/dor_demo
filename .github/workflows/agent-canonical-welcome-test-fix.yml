name: Agent Canonical Welcome Test Fix

on:
  push:
    branches:
      - agent/ui-business-closure-20260804
    paths:
      - scripts/ci/apply_canonical_welcome_test_fix.py
      - .github/workflows/agent-canonical-welcome-test-fix.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ui-business-closure-20260804
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python3 scripts/ci/apply_canonical_welcome_test_fix.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "test: 统一欢迎语多语言对象契约"
          git push origin HEAD:agent/ui-business-closure-20260804
