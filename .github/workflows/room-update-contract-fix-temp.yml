name: Apply Room Update Contract Fix

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  apply:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary migration completed"
