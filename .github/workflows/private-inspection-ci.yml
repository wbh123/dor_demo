name: Private Inspection Compatibility CI

# private inspection refresh 10
on:
  push:
    branches:
      - 'private-compat/feat/inspection-workflow-skeleton-20260814'
permissions:
  contents: read
jobs:
  verify-inspection:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout private feature branch
        uses: actions/checkout@v5
        with:
          repository: wbh123/Wust-Dormitory-Select
          ref: feat/inspection-workflow-skeleton-20260814
          token: ${{ secrets.PRIVATE_CI }}
          path: private-repo
          persist-credentials: false
          fetch-depth: 1
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - name: Verify inspection OpenAPI contract
        working-directory: private-repo
        run: python scripts/ci/test_inspection_openapi_contract.py
      - name: Verify inspection contracts
        working-directory: private-repo/app
        run: node --experimental-strip-types --test src/inspection/state.spec.ts src/inspection/store.spec.ts src/platform/inspection-navigation.spec.ts src/platform/inspection-workflow-layout.spec.ts
      - name: Install and typecheck mobile app
        working-directory: private-repo/app
        run: |
          npm install --no-audit --no-fund
          npm run typecheck
