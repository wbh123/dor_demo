name: Private APP Workbench Scroll V2 Apply

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'One-shot private writer retired after candidate creation.'
