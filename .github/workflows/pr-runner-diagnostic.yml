name: PR runner diagnostic

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Verify runner startup
        run: |
          echo "runner-started"
          uname -a
