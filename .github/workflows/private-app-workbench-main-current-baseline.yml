name: Private APP Workbench Current Main Baseline

on:
  push:
    branches:
      - 'private-compat/fix/app-workbench-scroll-boundaries-v4-20260816'

permissions:
  contents: read

concurrency:
  group: private-app-workbench-current-main-${{ github.ref }}
  cancel-in-progress: true

jobs:
  baseline:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout current private main
        uses: actions/checkout@v5
        with:
          repository: wbh123/Wust-Dormitory-Select
          ref: a9dc5990fce227f40e43c08c613dcda24619680f
          token: ${{ secrets.PRIVATE_CI }}
          path: private-repo
          persist-credentials: false
          fetch-depth: 1
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
      - name: Install APP dependencies
        working-directory: private-repo/app
        run: npm ci
      - name: Run APP full tests on current main
        working-directory: private-repo/app
        run: npm test
