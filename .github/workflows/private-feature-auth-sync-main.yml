name: Private Feature Authorization Sync Main

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Candidate frozen after syncing main@27ab9a79."
