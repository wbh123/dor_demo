name: Private Feature Authorization DB Generation Fix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired after confirming pre-existing Navicat generator drift."
