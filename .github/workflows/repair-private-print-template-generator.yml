name: Repair private print template generator

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'One-shot repair already completed successfully; see run 31920186909.'
